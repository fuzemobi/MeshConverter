# Core dependencies (required)
open3d>=0.13.0
numpy>=1.21.0,<2.0.0
scipy>=1.7.0
scikit-learn>=1.0.0
matplotlib>=3.3.0

# Optional dependencies (cylinder detection)
# pyransac3d>=0.6.0  # Commented out - install separately if needed
